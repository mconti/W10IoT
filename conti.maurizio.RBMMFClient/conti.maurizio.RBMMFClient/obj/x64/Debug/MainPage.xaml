<Page x:ConnectionId='1'
    x:Class="conti.maurizio.RBMMFClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:conti.maurizio.RBMMFClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
                        
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Orientation="Horizontal">
            <Button x:ConnectionId='3' VerticalAlignment="Stretch" Width="100" Margin="5"                     >Publish</Button>
            <RelativePanel Margin="10" Width="100">
                <TextBlock x:ConnectionId='8' Name="lblTemperatura">Temperatura</TextBlock>
                <TextBox x:ConnectionId='9' Name="edtTemperatura" RelativePanel.Below="lblTemperatura" HorizontalAlignment="Stretch" Text="25"></TextBox>
            </RelativePanel>
            <RelativePanel Margin="10" Width="100">
                <TextBlock x:ConnectionId='6' Name="lblLuminosità">Luminosità</TextBlock>
                <TextBox x:ConnectionId='7' Name="edtLuminosita" RelativePanel.Below="lblLuminosità" HorizontalAlignment="Stretch" Text="45"></TextBox>
            </RelativePanel>
            <RelativePanel Margin="10" Width="100">
                <TextBlock x:ConnectionId='4' Name="lblRumore">Rumore</TextBlock>
                <TextBox x:ConnectionId='5' Name="edtRumore" RelativePanel.Below="lblRumore" HorizontalAlignment="Stretch" Text="15"></TextBox>
            </RelativePanel>
        </StackPanel>
        
        <ListView x:ConnectionId='2' Grid.Row="1" Grid.Column="0" Name="lvData" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="30">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="50" Text="{Binding Temperatura}" Style="{StaticResource BaseTextBlockStyle}" />
                        <TextBlock Width="50" Text="{Binding Luminosita}" Style="{StaticResource BaseTextBlockStyle}" />
                        <TextBlock Width="50" Text="{Binding Rumore}" Style="{StaticResource BaseTextBlockStyle}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <WebView Grid.Row="1" Grid.Column="1" Source="https://freeboard.io/board/rGme96"></WebView>
    </Grid>
</Page>

